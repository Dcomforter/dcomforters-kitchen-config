apiVersion: v1
kind: Service
metadata:
  name: dcomforters-kitchen-svc
  namespace: production
  labels:
    app: dcomforters-kitchen
spec:
  selector:
    app: dcomforters-kitchen
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
